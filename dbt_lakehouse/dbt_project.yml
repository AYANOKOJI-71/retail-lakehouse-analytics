name: retail_lakehouse
version: "0.1.0"
config-version: 2

profile: retail_lakehouse

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  retail_lakehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
